import RegistryManager from "@/components/RegistryManager";

export const metadata = { title: "Registry — Visa Risk Console" };

export default function RegistryPage() {
  return <RegistryManager />;
}
